{
  "name": "react-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify js/index.js -o build/test-bundle-BUILD.js; mv build/test-bundle-BUILD.js build/test-bundle.js",
    "watch": "watchify js/index.js -o build/test-bundle.js -v"
  },
  "author": "",
  "license": "ISC",
  "browser": {
    "jquery": "./bower_components/jquery/dist/jquery.min.js"
  },
  "browserify-shim": {
    "jquery": "$"
  },
  "browserify": {
    "transform": [
      "browserify-shim"
    ]
  },
  "dependencies": {
    "browserify": "^6.1.0",
    "lodash": "^2.4.1"
  },
  "devDependencies": {
    "browserify-shim": "^3.8.0",
    "karma": "^0.12.24",
    "karma-chrome-launcher": "^0.1.5",
    "karma-jasmine": "^0.1.5"
  }
}
